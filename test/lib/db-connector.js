var db = require('../../lib/db/db-connector.js'),
  should = require('should');
/*
describe('DB Connector', function() {
  it('should remove, insert, and find data over a collection', function(done) {
    db.problems.remove({});
    db.problems.insert({name: 'abc'});
    db.problems.findOne({name: 'abc'}, function(err, document) {
      should(err).not.be.ok;
      document.should.be.ok;
      document.should.have.property('name', 'abc');
    });
    done();
  });
});*/
